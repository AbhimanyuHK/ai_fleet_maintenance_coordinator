name: CI

on:
  push:
    branches: [main, phase-1-data-foundation, phase-2-data-foundation, phase-3-knowledge-foundation]
  pull_request:
    branches: [main]

jobs:
  test-and-data-quality:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -r requirements.txt
      - run: python -m pytest -q
      - run: python scripts/validate_phase1_data.py
      - run: python scripts/profile_phase1_data.py
      - run: python scripts/normalize_phase1_data.py
      - run: python scripts/build_phase1_curated.py
      - run: python scripts/generate_phase1_readiness_report.py
      - run: python scripts/validate_phase2_data.py
      - run: python scripts/build_phase3_knowledge.py
      - run: python scripts/evaluate_phase3_retrieval.py
      - run: python scripts/build_phase3_embeddings.py
      - uses: actions/upload-artifact@v4
        with:
          name: phase3-vector-index
          path: artifacts/phase3/vector_index/
